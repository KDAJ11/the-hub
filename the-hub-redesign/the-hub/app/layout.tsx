import type { Metadata } from 'next'
import './globals.css'
import Navbar from '@/components/Navbar'
import Footer from '@/components/Footer'

export const metadata: Metadata = {
  title: 'The Hub',
  description: 'A gathering of young, royal, and vibrant leaders. Events, community, and growth.',
  openGraph: {
    title: 'The Hub',
    description: 'A gathering of young, royal, and vibrant leaders.',
    type: 'website',
  },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
